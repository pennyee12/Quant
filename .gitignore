# macOS
.DS_Store

# local data cache — large binary JSON files, no need in repo
data/bars/

# local Schwab credentials — never commit
*.token.json

# local API credentials — never commit
.env
.env.*
!.env.example

# local trading runtime state/logs
alpaca_state.json
logs/

# build artifacts
/quant

# local claude context
.claude/

# editor
.vscode/
.idea/
